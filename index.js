//create a function expression that divides two numbers
const divide = function (a,b) {
    return a / b;
}
//create an arrow function that squares a number
const square = (x) => {
    return x ** 2;
}

//create an arrow function that adds two numbers
const add = (a, b) => {
    return a + b;
}